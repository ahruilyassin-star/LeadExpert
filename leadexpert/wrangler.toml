name = "leadexpert-scroll"
compatibility_date = "2024-09-23"

# Static-asset Worker: serves the scroll-cinematic site straight from this
# folder. Deployed in CI via .github/workflows/deploy-leadexpert-cf.yml using
# the existing CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID secrets.
# Live at: https://leadexpert-scroll.<your-subdomain>.workers.dev
[assets]
directory = "."
