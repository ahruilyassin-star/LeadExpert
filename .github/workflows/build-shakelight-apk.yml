name: Build ShakeLight APK

on:
  push:
    branches: [ main, claude/shake-light-stability-luxbtx ]
    paths:
      - 'shake-flashlight/**'
      - '.github/workflows/build-shakelight-apk.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'shake-flashlight/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.4'

      - name: Generate Gradle wrapper
        working-directory: shake-flashlight
        run: gradle wrapper --gradle-version 8.4

      - name: Build Debug APK
        working-directory: shake-flashlight
        run: ./gradlew assembleDebug --no-daemon
        env:
          ANDROID_HOME: /usr/local/lib/android/sdk

      - name: Rename APK
        run: |
          cp shake-flashlight/app/build/outputs/apk/debug/app-debug.apk \
             shake-flashlight/app/build/outputs/apk/debug/ShakeLight.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ShakeLight-debug-apk
          path: shake-flashlight/app/build/outputs/apk/debug/ShakeLight.apk
          retention-days: 30
          if-no-files-found: error

      - name: Publish GitHub Release (publieke downloadlink)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: shakelight-latest
          name: "ShakeLight — Laatste versie"
          body: |
            ## ShakeLight APK

            **Direct installeren op je Android telefoon:**
            1. Download `ShakeLight.apk` hieronder
            2. Open het bestand op je telefoon
            3. Sta installatie toe als gevraagd (Instellingen → Onbekende bronnen)
            4. Installeer en open ShakeLight

            ---
            *Automatisch gebouwd vanuit de broncode*
          files: shake-flashlight/app/build/outputs/apk/debug/ShakeLight.apk
          make_latest: true
          prerelease: false
