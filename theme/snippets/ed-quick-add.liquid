{% comment %}
  ed-quick-add — DISABLED

  This snippet previously injected a SECOND "Añadir al carrito" button
  (class .ed-qa-btn, background #1a1a1a) onto every editorial product-grid
  card (.epg-card) via JavaScript.

  The theme already renders its own quick-add button ("AÑADIR AL CARRITO"
  with the bag icon, controlled by the theme's `quick_add` setting), so this
  custom button was a duplicate. It was removed at the store owner's request.
  The snippet now renders nothing; the native quick-add button remains.

  To restore the custom button, revert this file to its previous contents.
{% endcomment %}
